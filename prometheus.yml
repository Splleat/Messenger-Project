global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'messenger-app'
    metrics_path: '/actuator/prometheus'
    scrape_interval: 5s # 테스트를 위해서 5초마다 수집
    static_configs:
      - targets: ['host.docker.internal:8080']