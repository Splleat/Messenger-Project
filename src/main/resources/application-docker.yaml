spring:
  datasource:
    url: "jdbc:mysql://db:3306/my_db?allowPublicKeyRetrieval=true&useSSL=false"
    username: user
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          batch_size: 50
  data:
    redis:
      host: redis
      port: 6379
      connect-timeout: 2000ms
      
minio:
  endpoint: http://minio:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: messenger